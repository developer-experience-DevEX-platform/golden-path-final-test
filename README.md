# golden-path-final-test

Use one completely fresh service and make no manual repo changes unless the test proves the platform still requires one.

Owner: group:default/developer-experience

Install: `npm install`

Run in development: `npm run dev`
